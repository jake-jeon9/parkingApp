<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.boardMapper">
	<!-- 보드 -->
	<!-- 보드 추가 -->
	<insert id="costInsert" parameterType="int" >
		INSERT INTO cost 
		VALUES     #{memberNo}
		           DEFAULT,
				   DEFAULT,
				   DEFAULT,
				   DEFAULT,
				   DEFAULT,
				   DEFAULT,
				   DEFAULT,
				   SYSDATE)
	</insert>
	<!-- 보드 수정 -->
	<update id="boardModify" parameterType="costDTO" >
		UPDATE cost 
		SET    additionalCost = #{additionalCost}, 
			   additionalTiem=#{additionalTiem},
			   baseCost=#{baseCost},
			   baseTime=#{baseTime},
			   maxcost=#{maxcost},
			   maxtime=#{maxtime},
			   maxArea=#{maxArea},
		       sysdate 
		WHERE  memberNo = #{memberNo} 
	</update>
	<!-- 보드 삭제 -->
		<delete id="boardDelete" parameterType="boardDTO" >
		DELETE FROM cost 
		WHERE  memberNo = #{memberNo}
	</delete>
</mapper>